const page_path = './../src/page/';

const primary_scripts =
[
    
];

const secondary_scripts =
[
    page_path + 'template/header.js',
    page_path + 'template/footer.js',
    page_path + 'index/content.js',
];

const scipts_to_load = [primary_scripts, secondary_scripts];